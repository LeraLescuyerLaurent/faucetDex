{
  "routes": [
    {
      "method": "GET",
      "path": "/gains",
      "handler": "gain.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gains/count",
      "handler": "gain.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gains/:id",
      "handler": "gain.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gains",
      "handler": "gain.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gains/:id",
      "handler": "gain.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gains/:id",
      "handler": "gain.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
